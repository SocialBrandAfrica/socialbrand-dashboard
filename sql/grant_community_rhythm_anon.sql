-- community_rhythm table was created with RLS "anon read" policy but no
-- GRANT SELECT. The anon key cannot read the table without the grant.
-- Run once in Supabase SQL Editor.

GRANT SELECT ON public.community_rhythm TO anon;
