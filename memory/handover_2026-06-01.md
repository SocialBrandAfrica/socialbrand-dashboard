---
name: handover_2026-06-01
description: 2026-06-01 session handover — SB-AP-003 partial deploy, EAN mismatch, PM decision pending
metadata:
  type: handover
---

## Done this session

### Housekeeping (session 3)
- DIWAAIS root cleaned: archive/{handovers,briefs,evidence,scratch} created; 41 handovers + 43 briefs + 52 working files moved
- ARCHIVE-INDEX.md (SB-INDEX-007) + BUG-LOG.md created
- All 6 canonical files given Class: CANONICAL headers

### SB-AP-003 — production classification (session 4, commit 8c9ca43)
- A1: product_classification table created and verified
- A2: 13,321 rows loaded for store 10116 (249 AUTO_EXCLUDE, 732 REVIEW, 12,340 STOCK)
- A3: v_kpi_by_date rebuilt with ghost_stock_value column; rpc_kpi_dept_counts + rpc_top20 updated
- mv_kpi_by_date NOT rebuilt — timed out in Supabase editor
- A5, A6 SQL not run yet
- Frontend: ghost_stock_value in Capital Tied tooltip + Ghost Stock report in drawer

### SB-AP-003 EAN mismatch (session 5)
- ghost_stock_value shows R68.17 — classifier's AUTO_EXCLUDE items don't join to daily_snapshots
- Root cause: product_reference_import.csv (DIWAAIS2) uses PLU 209719 for WHOLE BEEF A-GRADE; PRSSALE uses PLU 209519 — different numbering range for scale/butchery items
- Ghost stock CONFIRMED from daily_snapshots: ~R7.9M in whole carcasses + bakery ingredients in slow-mover bucket
- DIWAAIS2↔PRSSALE scope: 501 unmatched EANs, net −R224k (not material). Product_catalog ↔ daily_snapshots ~96% aligned — mismatch is ONLY in the classifier input file, not in production joins
- internal_ref/sigma_product_code bridge does NOT resolve the mismatch (833 synthetic PLU items still unmatched via that key)
- Hardcoding store-specific EANs rejected by Pieter (correct — breaks multi-store)
- PM decision pending: Path A (classify from daily_snapshots signals) vs Path B (re-run classifier using product_catalog as input — CC recommends)

## State right now

| Object | State |
|---|---|
| product_classification | Loaded, 13,321 rows, AUTO_EXCLUDE=249 but EAN mismatch |
| v_kpi_by_date | Rebuilt with ghost_stock_value; shows R68.17 only |
| mv_kpi_by_date | Old version still live (not rebuilt) |
| v_production_review_queue | Not yet created |
| rpc_confirm_production | Not yet created |
| rpc_ghost_stock_report | Not yet created |

## Pending (in priority order)

1. PM decides Path A vs B for classifier fix
2. If Path B: CC builds product_catalog-based classifier input + re-runs production_classifier.py
3. A3 mv_kpi_by_date rebuild (pg_cron approach — avoid Supabase editor timeout)
4. A5 + A6 SQL run
5. SEL-001 Part 3 (dept capital_tied) — after A3 fully deployed
